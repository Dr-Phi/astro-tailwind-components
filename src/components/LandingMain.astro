---
const { title } = Astro.props;
---

<div>
  <h3 class="font-semibold text-3xl text-center text-grayish mb-4">{title}</h3>
  <p class="text-clr-light-gray mb-10 text-center">
    <slot />
  </p>
  <button
    class="bg-aquamarina text-white w-full rounded-3xl font-semibold py-[0.7rem] shadow-lg mb-6 hover:scale-95"
    >Download for iOS</button
  >
  <button
    class="bg-clr-light-blue text-white w-full rounded-3xl font-semibold py-[0.7rem] shadow-lg hover:scale-95"
    >Download for Mac</button
  >
</div>
